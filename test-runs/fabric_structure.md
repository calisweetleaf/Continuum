# Class Index for `reactive_attention_fabric.py`

Generated by `analyze_python_structure.py` using Python's AST; the source is never imported or executed.

- Class declarations: 50 (50 top-level, 0 nested)
- Definitions emitted (module-level functions and class methods): 92

## class `AttentionFabricError`(Exception) (lines 68-82)
**Doc**: Base domain error for the reactive attention fabric.

- `def __init__(self, message: str, remediation: str | None=None) -> None:` (lines 73-82)
  - Initialize a fabric error with operator remediation.

## class `InvalidStreamError`(AttentionFabricError) (lines 85-88)
**Doc**: Raised when a stream event, adapter ingress, or query is malformed.

- _No methods._

## class `MemoryBoundaryError`(AttentionFabricError) (lines 91-94)
**Doc**: Raised when durable memory cannot be read or written.

- _No methods._

## class `AdapterError`(AttentionFabricError) (lines 97-100)
**Doc**: Raised when a stream adapter cannot project native source material.

- _No methods._

## class `KernelError`(AttentionFabricError) (lines 103-106)
**Doc**: Raised when an attention kernel cannot legally run on the candidate set.

- _No methods._

## class `TopologyError`(AttentionFabricError) (lines 109-112)
**Doc**: Raised when topology selection or kernel registration is invalid.

- _No methods._

## class `AttentionRole`(str, Enum) (lines 115-123)
**Doc**: Surviving Morpheus stream roles, now independent of model modality.

- _No methods._

## class `PersistencePolicy`(str, Enum) (lines 126-132)
**Doc**: How long a stream's native source is expected to remain addressable.

- _No methods._

## class `FabricEventKind`(str, Enum) (lines 135-144)
**Doc**: Mailbox verbs that can reallocate attention without a full restart.

- _No methods._

## class `StreamDormancy`(str, Enum) (lines 147-151)
**Doc**: Whether a stream currently occupies active context.

- _No methods._

## class `ContentRef` (lines 155-178)
**Doc**: Address into native source material. Never a replacement for the source.

- `def as_dict(self) -> dict[str, str | int | None]:` (lines 165-178)
  - Serialize the reference without inlining native bytes.

## class `ProvenanceStamp` (lines 182-201)
**Doc**: Origin metadata that must survive projection and context assembly.

- `def as_dict(self) -> dict[str, object]:` (lines 190-201)
  - Serialize provenance for durable storage.

## class `NativeIngress` (lines 205-212)
**Doc**: Native environmental material before any attentional projection.

- _No methods._

## class `StreamEvent` (lines 216-256)
**Doc**: One typed emission from an environmental or cognitive stream.

- `def rank(self) -> float:` (lines 242-256)
  - Return the hard-selection rank used before expensive kernels.

## class `AttentionCandidate` (lines 260-293)
**Doc**: A selectable unit that may remain a reference until hard-selected.

- `def rank(self) -> float:` (lines 279-293)
  - Return metadata rank for staged selection.

## class `MemoryQuery` (lines 297-305)
**Doc**: Request sent across the memory boundary. Not an assembled context.

- _No methods._

## class `MemoryCandidate` (lines 309-321)
**Doc**: Durable evidence returned by an external memory runtime.

- _No methods._

## class `DurableRecord` (lines 325-336)
**Doc**: What the fabric may ask memory to keep. Never an active context blob.

- _No methods._

## class `MemoryReceipt` (lines 340-345)
**Doc**: Proof that durable memory accepted a record without consuming context.

- _No methods._

## class `ResourcePressure` (lines 349-397)
**Doc**: Live resource state that participates in topology and budget decisions.

- `def severity(self) -> float:` (lines 359-376)
  - Return a causal pressure scalar used by topology selection.
- `def active_budget(self) -> int:` (lines 378-397)
  - Return how many candidates may become active under this pressure.

## class `WorkMeter` (lines 401-424)
**Doc**: Honest accounting of work performed versus work avoided.

- `def quadratic_pairs_possible(self, n_query: int, n_key: int) -> int:` (lines 414-424)
  - Return the dense QK pair count that a false 'flash' kernel would pay.

## class `AttentionReceipt` (lines 428-445)
**Doc**: Trace from native streams to the attended view, including lossy steps.

- _No methods._

## class `ContextSlot` (lines 449-457)
**Doc**: One active-context cell. Reconstructible from the native reference.

- _No methods._

## class `ActiveContext` (lines 461-468)
**Doc**: Temporary projection assembled for present cognition. Not memory.

- _No methods._

## class `KernelResult` (lines 472-480)
**Doc**: Output of a single selected attention kernel.

- _No methods._

## class `AttendedCognitiveView` (lines 484-494)
**Doc**: The fabric's product: an attended view plus provenance, not a model token.

- _No methods._

## class `NumericAttendResult` (lines 498-503)
**Doc**: PyTorch-shaped numeric path that still carries a receipt.

- _No methods._

## class `PathStreamSummary` (lines 507-517)
**Doc**: Bounded filesystem-stream inspection used by the harness describe_path gate.

- _No methods._

## class `AttendPlan` (lines 521-533)
**Doc**: Snapshot of routing decisions taken before the selected kernel runs.

- _No methods._

## class `AttentionQuery` (lines 537-551)
**Doc**: An explicit ask against currently observed streams and/or supplied candidates.

- _No methods._

## class `ObserveResult` (lines 555-563)
**Doc**: Outcome of ingesting one event into the resident fabric.

- _No methods._

## class `StreamState` (lines 567-580)
**Doc**: Mutable live state for one registered stream.

- _No methods._

## class `MemoryRuntime`(Protocol) (lines 583-604)
**Doc**: Durable memory owned outside the fabric. Context must not be written here.

- `def recall(self, query: MemoryQuery) -> tuple[MemoryCandidate, ...]:` (lines 586-594)
  - Return durable candidates without assembling active context.
- `def remember(self, record: DurableRecord) -> MemoryReceipt:` (lines 596-604)
  - Persist a record that still points at native source.

## class `StreamAdapter`(Protocol) (lines 607-628)
**Doc**: Transforms native source into typed attentional addresses without erasing it.

- `def supports(self, ingress: NativeIngress) -> bool:` (lines 610-618)
  - Return whether this adapter can address the native ingress.
- `def ingest(self, ingress: NativeIngress) -> tuple[StreamEvent, ...]:` (lines 620-628)
  - Emit stream events whose content_ref still names the native source.

## class `AttentionKernel`(Protocol) (lines 631-652)
**Doc**: One attention topology with an honest complexity class.

- `def attend(self, queries: Sequence[tuple[float, ...]], candidates: Sequence[AttentionCandidate], abort: Callable[[], bool]) -> KernelResult:` (lines 637-652)
  - Run this kernel only. Callers must not mix sibling kernels afterward.

## class `FileStreamAdapter` (lines 978-1113)
**Doc**: Addresses files and directories without ingesting them as token sequences.

- `def supports(self, ingress: NativeIngress) -> bool:` (lines 981-997)
  - Accept filesystem, file, and directory domains, or existing paths.
- `def ingest(self, ingress: NativeIngress) -> tuple[StreamEvent, ...]:` (lines 999-1022)
  - Emit file or directory events that keep the path as the source of truth.
- `def _ingest_directory(self, path: Path, ingress: NativeIngress, stamp: ProvenanceStamp) -> tuple[StreamEvent, ...]:` (lines 1024-1074)
  - Emit a directory event and bounded child file references.
- `def _ingest_file(self, path: Path, ingress: NativeIngress, stamp: ProvenanceStamp, stream_id: str) -> StreamEvent:` (lines 1076-1113)
  - Address one file. Head bytes may be hashed; the file is not tokenized.

## class `SqliteStreamAdapter` (lines 1116-1255)
**Doc**: Addresses sqlite databases as row/table streams without tensorizing the DB.

- `def supports(self, ingress: NativeIngress) -> bool:` (lines 1119-1131)
  - Accept sqlite/database domains or .sqlite/.db locators.
- `def ingest(self, ingress: NativeIngress) -> tuple[StreamEvent, ...]:` (lines 1133-1205)
  - Emit a database event plus one event per table, leaving rows as rowids.
- `def iter_row_candidates(self, db_path: str, table: str, limit: int | None=None) -> tuple[AttentionCandidate, ...]:` (lines 1207-1255)
  - Yield row candidates as sqlite rowid references without loading blobs.

## class `StructuredObjectAdapter` (lines 1258-1325)
**Doc**: Addresses in-process structured objects, tool results, receipts, and diffs.

- `def supports(self, ingress: NativeIngress) -> bool:` (lines 1283-1292)
  - Accept typed object domains, including user-defined custom streams.
- `def ingest(self, ingress: NativeIngress) -> tuple[StreamEvent, ...]:` (lines 1294-1325)
  - Keep the object as payload when present and always emit a content_ref.

## class `SqliteDurableMemory` (lines 1344-1506)
**Doc**: SQLite-backed memory runtime. Stores records and refs, never active context.

- `def __init__(self, db_path: str) -> None:` (lines 1347-1381)
  - Open or create a provenance-preserving memory database.
- `def remember(self, record: DurableRecord) -> MemoryReceipt:` (lines 1383-1429)
  - Persist a record that still points at native source.
- `def recall(self, query: MemoryQuery) -> tuple[MemoryCandidate, ...]:` (lines 1431-1506)
  - Return durable candidates without assembling active context.

## class `ExactDenseAttention` (lines 1509-1562)
**Doc**: Exact softmax attention. Arithmetic is O(N^2). Use only on small sets.

- `def attend(self, queries: Sequence[tuple[float, ...]], candidates: Sequence[AttentionCandidate], abort: Callable[[], bool]) -> KernelResult:` (lines 1515-1562)
  - Compare every query to every remaining key.

## class `BlockStreamingExactAttention` (lines 1565-1629)
**Doc**: Exact attention with chunked Q. Arithmetic remains O(N^2); working memory is O(chunk * N).

- `def __init__(self, chunk_size: int=DEFAULT_CHUNK_SIZE) -> None:` (lines 1575-1581)
  - Configure the query chunk used to bound working memory.
- `def attend(self, queries: Sequence[tuple[float, ...]], candidates: Sequence[AttentionCandidate], abort: Callable[[], bool]) -> KernelResult:` (lines 1583-1629)
  - Stream query chunks against the full key set.

## class `LinearKernelAttention` (lines 1632-1706)
**Doc**: ELU+1 feature-map attention. Arithmetic is O((N+Q) d^2), linear in sequence length.

- `def attend(self, queries: Sequence[tuple[float, ...]], candidates: Sequence[AttentionCandidate], abort: Callable[[], bool]) -> KernelResult:` (lines 1642-1706)
  - Accumulate phi(K)^T V once, then query it with phi(Q).

## class `SparseTopKAttention` (lines 1720-1777)
**Doc**: Hard top-k routing then exact attention on the survivors. Skips unselected keys.

- `def __init__(self, top_k: int=DEFAULT_TOP_K) -> None:` (lines 1726-1732)
  - Configure the hard selection width.
- `def attend(self, queries: Sequence[tuple[float, ...]], candidates: Sequence[AttentionCandidate], abort: Callable[[], bool]) -> KernelResult:` (lines 1734-1777)
  - Score all keys cheaply, keep k, then run exact attention only on those k.

## class `RetrievalFirstAttention` (lines 1780-1832)
**Doc**: Metadata filter, then hard top-k, then exact attention. Required for huge stores.

- `def __init__(self, top_k: int=DEFAULT_TOP_K, min_rank: float=-1000000000.0) -> None:` (lines 1786-1794)
  - Configure staged narrowing.
- `def attend(self, queries: Sequence[tuple[float, ...]], candidates: Sequence[AttentionCandidate], abort: Callable[[], bool]) -> KernelResult:` (lines 1796-1832)
  - Drop candidates on metadata before any pairwise neural attention.

## class `HierarchicalMultiscaleAttention` (lines 1835-1926)
**Doc**: Coarse block attention, then fine attention only inside high-mass blocks.

- `def __init__(self, block_size: int=DEFAULT_BLOCK_SIZE, keep_blocks: int=DEFAULT_HIERARCHICAL_BLOCKS) -> None:` (lines 1841-1849)
  - Configure the multiscale partition.
- `def attend(self, queries: Sequence[tuple[float, ...]], candidates: Sequence[AttentionCandidate], abort: Callable[[], bool]) -> KernelResult:` (lines 1851-1926)
  - Attend to block summaries, then expand only the winning blocks.

## class `CrossStreamAttention` (lines 1929-2021)
**Doc**: Attend among stream summaries, then expand only streams that survive hard routing.

- `def __init__(self, keep_streams: int=3, inner_k: int=16) -> None:` (lines 1935-1943)
  - Configure cross-stream hard routing.
- `def attend(self, queries: Sequence[tuple[float, ...]], candidates: Sequence[AttentionCandidate], abort: Callable[[], bool]) -> KernelResult:` (lines 1945-2021)
  - Drop whole streams before paying event-level attention inside them.

## class `TemporalEventAttention` (lines 2024-2082)
**Doc**: Recency- and urgency-weighted hard selection over an event timeline.

- `def __init__(self, top_k: int=DEFAULT_TOP_K, half_life_s: float=RECENCY_HALF_LIFE_S) -> None:` (lines 2030-2038)
  - Configure temporal decay.
- `def attend(self, queries: Sequence[tuple[float, ...]], candidates: Sequence[AttentionCandidate], abort: Callable[[], bool]) -> KernelResult:` (lines 2040-2082)
  - Score by recency, urgency, and novelty, then exact-attend the winners.

## class `TopologySelector` (lines 2097-2159)
**Doc**: Hard topology choice. Exactly one kernel runs; siblings are not evaluated.

- `def select(self, candidate_count: int, stream_count: int, pressure: ResourcePressure, forced: TopologyName | None=None) -> tuple[TopologyName, str, tuple[str, ...]]:` (lines 2100-2159)
  - Choose one topology from pressure, population, and stream cardinality.

## class `EventMailbox` (lines 2162-2228)
**Doc**: Thread-safe event mailbox. Not an application server.

- `def __init__(self) -> None:` (lines 2165-2168)
  - Create an empty mailbox.
- `def push(self, event: StreamEvent) -> None:` (lines 2170-2178)
  - Enqueue an event. Higher rank is served first.
- `def pop(self) -> StreamEvent | None:` (lines 2180-2189)
  - Pop the highest-rank pending event.
- `def peek_rank(self) -> float:` (lines 2191-2200)
  - Return the highest pending rank, or 0 when empty.
- `def depth(self) -> int:` (lines 2202-2209)
  - Return current queue depth.
- `def drain(self, limit: int | None=None) -> tuple[StreamEvent, ...]:` (lines 2211-2228)
  - Pop up to limit events.

## class `ReactiveAttentionFabric` (lines 2231-2774)
**Doc**: Model-independent reactive attention fabric.

- `def __init__(self, memory: MemoryRuntime | None=None, memory_path: str | None=None, dense_limit: int=DEFAULT_DENSE_LIMIT) -> None:` (lines 2238-2277)
  - Create a resident fabric with default adapters and honest kernels.
- `def register_adapter(self, adapter: StreamAdapter) -> None:` (lines 2279-2286)
  - Register a stream adapter. Earlier adapters win on first supports() match.
- `def register_kernel(self, kernel: AttentionKernel) -> None:` (lines 2288-2310)
  - Register or replace one topology kernel. Used for optimized backends.
- `def remember(self, record: DurableRecord) -> MemoryReceipt:` (lines 2312-2321)
  - Write durable evidence through the memory boundary.
- `def recall(self, query: MemoryQuery) -> tuple[MemoryCandidate, ...]:` (lines 2323-2332)
  - Ask the memory runtime for candidates. Does not assemble context.
- `def context_snapshot(self) -> ActiveContext | None:` (lines 2334-2341)
  - Return the current active context without touching durable memory.
- `def ingest_native(self, ingress: NativeIngress) -> tuple[StreamEvent, ...]:` (lines 2343-2359)
  - Adapt native material and observe the resulting events.
- `def observe(self, event: StreamEvent) -> ObserveResult:` (lines 2361-2427)
  - Admit one event. May wake a stream or request preemption of in-flight work.
- `def drain(self, budget: int=32) -> AttendedCognitiveView:` (lines 2429-2441)
  - Absorb pending mailbox events and assemble an attended view.
- `def attend(self, query: AttentionQuery) -> AttendedCognitiveView:` (lines 2443-2463)
  - Select a topology, avoid unselected work, and emit a provenanced view.
- `def forward(self, inputs: Mapping[str, object] | tuple[object, object, object], latency_budget_ms: float=100.0) -> NumericAttendResult:` (lines 2465-2538)
  - Conventional numeric/QKV path for PyTorch interoperability.
- `def _plan_attend(self, query: AttentionQuery) -> AttendPlan:` (lines 2540-2581)
  - Snapshot candidates and choose exactly one kernel. Caller holds the lock.
- `def _instantiate_kernel(self, topology: TopologyName, budget: int) -> AttentionKernel:` (lines 2583-2600)
  - Build a one-shot kernel so registered instances are not mutated underfoot.
- `def _commit_view(self, query: AttentionQuery, plan: AttendPlan, result: KernelResult, late_candidates: tuple[AttentionCandidate, ...]=()) -> AttendedCognitiveView:` (lines 2602-2684)
  - Assemble context from kernel output. Caller holds the lock.
- `def _collect_candidates(self, query: AttentionQuery) -> list[AttentionCandidate]:` (lines 2686-2712)
  - Assemble live, extra, and optional memory candidates without materializing sources.
- `def _metadata_filter(self, candidates: Sequence[AttentionCandidate], query: AttentionQuery, budget: int) -> list[AttentionCandidate]:` (lines 2714-2730)
  - Hard-filter by domain/stream and drop the worst ranks before any kernel.
- `def _query_vectors(self, query: AttentionQuery, candidates: Sequence[AttentionCandidate]) -> list[tuple[float, ...]]:` (lines 2732-2745)
  - Build query vectors from the ask, or from candidate metadata if needed.
- `def _ensure_stream(self, event: StreamEvent) -> StreamState:` (lines 2747-2761)
  - Create or return the live stream state for an event.
- `def _adapter_for(self, ingress: NativeIngress) -> StreamAdapter:` (lines 2763-2768)
  - Return the first adapter that supports native ingress.
- `def _should_abort(self) -> bool:` (lines 2770-2774)
  - Return True when a higher-rank event requested preemption.

## Module-level functions

- `def _clamp01(value: float) -> float:` (lines 655-661)
  - Clamp a scalar into [0, 1].
- `def _rank(priority: float, importance: float, novelty: float, urgency: float, uncertainty: float, cost: float, authority: float) -> float:` (lines 664-682)
  - Combine metadata into a hard-selection rank.
- `def _fit_dim(vector: Sequence[float], dim: int=VECTOR_DIM) -> tuple[float, ...]:` (lines 685-691)
  - Pad or crop a vector to the fabric's routing dimension.
- `def _dot(left: Sequence[float], right: Sequence[float]) -> float:` (lines 694-696)
  - Dot product over the overlapping prefix.
- `def _l2(vector: Sequence[float]) -> float:` (lines 699-701)
  - Euclidean norm.
- `def _normalize(vector: Sequence[float]) -> tuple[float, ...]:` (lines 704-707)
  - L2-normalize a vector.
- `def _mean_vectors(vectors: Sequence[Sequence[float]], dim: int=VECTOR_DIM) -> tuple[float, ...]:` (lines 710-720)
  - Mean of vectors, or zeros when empty.
- `def _softmax(values: Sequence[float]) -> tuple[float, ...]:` (lines 723-730)
  - Numerically stable softmax.
- `def _hashed_projection(material: str, dim: int=VECTOR_DIM) -> tuple[float, ...]:` (lines 733-745)
  - Cheap model-free projection from bytes/text. Not a replacement for the source.
- `def _metadata_vector(domain: str, priority: float, importance: float, novelty: float, urgency: float, uncertainty: float, cost: float, authority: float, timestamp: float, extra: str='') -> tuple[float, ...]:` (lines 748-776)
  - Build a routing vector from typed metadata when no embedding exists.
- `def _candidate_key(candidate: AttentionCandidate) -> tuple[float, ...]:` (lines 779-783)
  - Key vector used by kernels: projection when present, else routing vector.
- `def _candidate_value(candidate: AttentionCandidate) -> tuple[float, ...]:` (lines 786-788)
  - Value vector. Same as key unless a distinct projection exists.
- `def _top_k_indices(scores: Sequence[float], k: int) -> tuple[int, ...]:` (lines 791-796)
  - Return indices of the k largest scores. Hard selection, not a soft mix.
- `def _weighted_sum(weights: Sequence[float], vectors: Sequence[Sequence[float]]) -> tuple[float, ...]:` (lines 799-808)
  - Mix selected value vectors by attention weights.
- `def _json_safe(value: object) -> bool:` (lines 811-819)
  - Return True when a payload may be inlined into durable memory JSON.
- `def _event_to_candidate(event: StreamEvent) -> AttentionCandidate:` (lines 822-852)
  - Project a live event into a selectable candidate without copying native bytes.
- `def _memory_to_candidate(item: MemoryCandidate) -> AttentionCandidate:` (lines 855-868)
  - Address a durable memory record as an attention candidate.
- `def sample_resource_pressure(queue_depth: int=0, latency_budget_ms: float=100.0, candidate_population: int=0) -> ResourcePressure:` (lines 871-902)
  - Sample host pressure from procfs when present.
- `def _read_ram_used_ratio() -> float:` (lines 905-920)
  - Read MemAvailable/MemTotal from /proc/meminfo when the host provides it.
- `def _read_gpu_vram_ratio() -> float | None:` (lines 923-938)
  - Read CUDA memory pressure only when torch.cuda is actually available.
- `def _as_numeric_matrix(data: object) -> tuple[tuple[float, ...], ...]:` (lines 941-975)
  - Convert nested numeric objects, including optional torch tensors, to tuples.
- `def _render_payload(payload: object | None) -> str:` (lines 1328-1336)
  - Render a native payload to text for hashing without claiming to be the source.
- `def _stream_id_for(path: Path | str) -> str:` (lines 1339-1341)
  - Derive a stable stream id from a locator.
- `def _phi_elu(vector: Sequence[float]) -> tuple[float, ...]:` (lines 1709-1717)
  - Positive ELU+1 feature map used by linear kernel attention.
- `def _empty_kernel(complexity_class: str, lossy: tuple[str, ...]) -> KernelResult:` (lines 2085-2094)
  - Return a preempted or empty kernel result without inventing attention mass.
- `def describe_path(project_root: str) -> PathStreamSummary:` (lines 2777-2810)
  - Ingest a real filesystem path as a typed stream and return a bounded attention summary.
